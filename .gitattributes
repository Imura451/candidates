# 改行コードを git に書き換えさせない
#
# .bat は Shift-JIS(cp932) ＋ CRLF で保存してあり、書き換えられると
# 日本語Windowsのcmdが読めなくなります。写真やPDFも同じ理由で触らせません。
# この1行が無いと、公開のたびに「LF will be replaced by CRLF」の警告が
# 画面いっぱいに出ます（動作に害はありませんが、読みにくくなります）。
* -text
